服务端：
    1.当客户端练连接到服务器之后，会得到一个 socket
    2.启动一个线程，该线程只有 socket 对象
    3.为了更好的管理线程，需要使用一个集合 HashMap 来管理
客户端：
    1.和服务器端通讯时使用对象，可以使用对象流来读写
    2.当客户端连接到服务端后，也会得到 Socket
    3.启动线程，该线程持有 Socket，为了更好的管理线程，也将该线程放入集合
